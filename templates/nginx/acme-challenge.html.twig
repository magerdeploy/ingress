server {
    listen      80;
    listen      [::]:80;
    server_name {{ domain }};
    location = /.well-known/acme-challenge/{{ token }} {
        add_header Content-Type text/plain;
        return 200 '{{ payload }}';
    }
}}